///
/// JHybridNitroVideoViewStateUpdater.cpp
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#include "JHybridNitroVideoViewStateUpdater.hpp"
#include "views/HybridNitroVideoViewComponent.hpp"
#include <NitroModules/NitroDefines.hpp>
#include <react/fabric/StateWrapperImpl.h>

namespace margelo::nitro::nitrovideo::views {

using namespace facebook;
using ConcreteStateData = react::ConcreteState<HybridNitroVideoViewState>;

std::shared_ptr<const HybridNitroVideoViewProps> JHybridNitroVideoViewStateUpdater::getPropsFromStateWrapper(
    jni::alias_ref<JStateWrapper::javaobject> stateWrapper) {
  if (stateWrapper.get() == nullptr) {
    return nullptr;
  }
  // Get concrete StateWrapperImpl from passed StateWrapper interface object
  jobject rawStateWrapper = stateWrapper.get();
  if (!stateWrapper->isInstanceOf(react::StateWrapperImpl::javaClassStatic())) [[unlikely]] {
    throw std::runtime_error("StateWrapper is not a StateWrapperImpl");
  }
  auto stateWrapperImpl = jni::alias_ref<react::StateWrapperImpl::javaobject>{
    static_cast<react::StateWrapperImpl::javaobject>(rawStateWrapper)
  };
  std::shared_ptr<const react::State> state = stateWrapperImpl->cthis()->getState();
  if (state == nullptr) {
    return nullptr;
  }
  auto concreteState = std::static_pointer_cast<const ConcreteStateData>(state);
  const HybridNitroVideoViewState& data = concreteState->getData();
  const std::shared_ptr<const HybridNitroVideoViewProps>& props = data.getProps();
  if (props == nullptr) [[unlikely]] {
    throw std::runtime_error("HybridNitroVideoViewState's data doesn't contain any props!");
  }
  return props;
}

void JHybridNitroVideoViewStateUpdater::updateViewProps(jni::alias_ref<jni::JClass> /* class */,
                                           jni::alias_ref<JHybridNitroVideoViewSpec::JavaPart> javaView,
                                           jni::alias_ref<JStateWrapper::javaobject> newState,
                                           jni::alias_ref<JStateWrapper::javaobject> oldState) {
  std::shared_ptr<JHybridNitroVideoViewSpec> hybridView = javaView->getJHybridNitroVideoViewSpec();
  std::shared_ptr<const HybridNitroVideoViewProps> newProps = getPropsFromStateWrapper(newState);
  std::shared_ptr<const HybridNitroVideoViewProps> oldProps = getPropsFromStateWrapper(oldState);
  if (newProps == nullptr) [[unlikely]] {
    throw std::runtime_error("Current StateWrapper doesn't contain any props!");
  }

  // Update only props that differ from the previous State snapshot.
  if (oldProps == nullptr
        ? newProps->source.isProvided()
        : !newProps->source.hasSameValue(oldProps->source)) {
    hybridView->setSource(newProps->source.get());
  }
  if (oldProps == nullptr
        ? newProps->paused.isProvided()
        : !newProps->paused.hasSameValue(oldProps->paused)) {
    hybridView->setPaused(newProps->paused.get());
  }
  if (oldProps == nullptr
        ? newProps->muted.isProvided()
        : !newProps->muted.hasSameValue(oldProps->muted)) {
    hybridView->setMuted(newProps->muted.get());
  }
  if (oldProps == nullptr
        ? newProps->repeat.isProvided()
        : !newProps->repeat.hasSameValue(oldProps->repeat)) {
    hybridView->setRepeat(newProps->repeat.get());
  }
  if (oldProps == nullptr
        ? newProps->volume.isProvided()
        : !newProps->volume.hasSameValue(oldProps->volume)) {
    hybridView->setVolume(newProps->volume.get());
  }
  if (oldProps == nullptr
        ? newProps->resizeMode.isProvided()
        : !newProps->resizeMode.hasSameValue(oldProps->resizeMode)) {
    hybridView->setResizeMode(newProps->resizeMode.get());
  }
  if (oldProps == nullptr
        ? newProps->onLoad.isProvided()
        : !newProps->onLoad.hasSameValue(oldProps->onLoad)) {
    hybridView->setOnLoad(newProps->onLoad.get());
  }
  if (oldProps == nullptr
        ? newProps->onProgress.isProvided()
        : !newProps->onProgress.hasSameValue(oldProps->onProgress)) {
    hybridView->setOnProgress(newProps->onProgress.get());
  }
  if (oldProps == nullptr
        ? newProps->onEnd.isProvided()
        : !newProps->onEnd.hasSameValue(oldProps->onEnd)) {
    hybridView->setOnEnd(newProps->onEnd.get());
  }
  if (oldProps == nullptr
        ? newProps->onError.isProvided()
        : !newProps->onError.hasSameValue(oldProps->onError)) {
    hybridView->setOnError(newProps->onError.get());
  }

  // Update hybridRef if it changed
  if (oldProps == nullptr
        ? newProps->hybridRef.isProvided()
        : !newProps->hybridRef.hasSameValue(oldProps->hybridRef)) {
    // hybridRef changed - call it with new this
    const auto& maybeFunc = newProps->hybridRef.get();
    if (maybeFunc.has_value()) {
      maybeFunc.value()(hybridView);
    }
  }
}

} // namespace margelo::nitro::nitrovideo::views
