///
/// HybridNitroVideoViewComponent.mm
/// This file was generated by nitrogen. DO NOT MODIFY THIS FILE.
/// https://github.com/mrousavy/nitro
/// Copyright © Marc Rousavy @ Margelo
///

#import "HybridNitroVideoViewComponent.hpp"
#import <memory>
#import <react/renderer/componentregistry/ComponentDescriptorProvider.h>
#import <React/RCTViewComponentView.h>
#import <React/RCTComponentViewFactory.h>
#import <React/UIView+ComponentViewProtocol.h>
#import <NitroModules/NitroDefines.hpp>
#import <UIKit/UIKit.h>

#import "HybridNitroVideoViewSpecSwift.hpp"
#import "NitroVideo-Swift-Cxx-Umbrella.hpp"

#if __has_include(<cxxreact/ReactNativeVersion.h>)
#include <cxxreact/ReactNativeVersion.h>
#if REACT_NATIVE_VERSION_MINOR >= 82
#define ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
#endif
#endif

using namespace facebook;
using namespace margelo::nitro::nitrovideo;
using namespace margelo::nitro::nitrovideo::views;

/**
 * Represents the React Native View holder for the Nitro "NitroVideoView" HybridView.
 */
@interface HybridNitroVideoViewComponent: RCTViewComponentView
+ (BOOL)shouldBeRecycled;
@end

@implementation HybridNitroVideoViewComponent {
  std::shared_ptr<HybridNitroVideoViewSpecSwift> _hybridView;
  BOOL _didDropView;
}

+ (void) load {
  [super load];
  [RCTComponentViewFactory.currentComponentViewFactory registerComponentViewClass:[HybridNitroVideoViewComponent class]];
}

+ (react::ComponentDescriptorProvider) componentDescriptorProvider {
  return react::concreteComponentDescriptorProvider<HybridNitroVideoViewComponentDescriptor>();
}

- (instancetype) init {
  if (self = [super init]) {
    _props = HybridNitroVideoViewShadowNode::defaultSharedProps();
    std::shared_ptr<HybridNitroVideoViewSpec> hybridView = NitroVideo::NitroVideoAutolinking::createNitroVideoView();
    _hybridView = std::dynamic_pointer_cast<HybridNitroVideoViewSpecSwift>(hybridView);
    [self updateView];
  }
  return self;
}

- (void) updateView {
  // 1. Get Swift part
  NitroVideo::HybridNitroVideoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Get UIView*
  void* viewUnsafe = swiftPart.getView();
  UIView* view = (__bridge_transfer UIView*) viewUnsafe;

  // 3. Update RCTViewComponentView's [contentView]
  [self setContentView:view];
}

- (void) notifyOnDropView {
  // A recycled component can later be invalidated. Notify only once per mount.
  if (_didDropView) {
    return;
  }
  NitroVideo::HybridNitroVideoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.onDropView();
  _didDropView = YES;
}

- (void) updateProps:(const std::shared_ptr<const react::Props>&)props
            oldProps:(const std::shared_ptr<const react::Props>&)oldProps {
  // A props update marks a newly mounted or still-active component.
  _didDropView = NO;

  // 1. Downcast props
  const auto& newViewProps = *std::static_pointer_cast<const HybridNitroVideoViewProps>(props);
  const auto* oldViewProps = static_cast<const HybridNitroVideoViewProps*>(oldProps.get());
  NitroVideo::HybridNitroVideoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();

  // 2. Update only props that differ from the previous Props snapshot.
  const bool hasTransactionPropChanges = oldViewProps == nullptr
      ? newViewProps.hasAnyProvidedProps()
      : !newViewProps.hasSameProps(*oldViewProps);
  if (hasTransactionPropChanges) {
    swiftPart.beforeUpdate();

    // source: string
    if (oldViewProps == nullptr
          ? newViewProps.source.isProvided()
          : !newViewProps.source.hasSameValue(oldViewProps->source)) {
      swiftPart.setSource(newViewProps.source.get());
    }
    // paused: optional
    if (oldViewProps == nullptr
          ? newViewProps.paused.isProvided()
          : !newViewProps.paused.hasSameValue(oldViewProps->paused)) {
      swiftPart.setPaused(newViewProps.paused.get());
    }
    // muted: optional
    if (oldViewProps == nullptr
          ? newViewProps.muted.isProvided()
          : !newViewProps.muted.hasSameValue(oldViewProps->muted)) {
      swiftPart.setMuted(newViewProps.muted.get());
    }
    // repeat: optional
    if (oldViewProps == nullptr
          ? newViewProps.repeat.isProvided()
          : !newViewProps.repeat.hasSameValue(oldViewProps->repeat)) {
      swiftPart.setRepeat(newViewProps.repeat.get());
    }
    // volume: optional
    if (oldViewProps == nullptr
          ? newViewProps.volume.isProvided()
          : !newViewProps.volume.hasSameValue(oldViewProps->volume)) {
      swiftPart.setVolume(newViewProps.volume.get());
    }
    // resizeMode: optional
    if (oldViewProps == nullptr
          ? newViewProps.resizeMode.isProvided()
          : !newViewProps.resizeMode.hasSameValue(oldViewProps->resizeMode)) {
      swiftPart.setResizeMode(newViewProps.resizeMode.get());
    }
    // onLoad: optional
    if (oldViewProps == nullptr
          ? newViewProps.onLoad.isProvided()
          : !newViewProps.onLoad.hasSameValue(oldViewProps->onLoad)) {
      swiftPart.setOnLoad(newViewProps.onLoad.get());
    }
    // onProgress: optional
    if (oldViewProps == nullptr
          ? newViewProps.onProgress.isProvided()
          : !newViewProps.onProgress.hasSameValue(oldViewProps->onProgress)) {
      swiftPart.setOnProgress(newViewProps.onProgress.get());
    }
    // onEnd: optional
    if (oldViewProps == nullptr
          ? newViewProps.onEnd.isProvided()
          : !newViewProps.onEnd.hasSameValue(oldViewProps->onEnd)) {
      swiftPart.setOnEnd(newViewProps.onEnd.get());
    }
    // onError: optional
    if (oldViewProps == nullptr
          ? newViewProps.onError.isProvided()
          : !newViewProps.onError.hasSameValue(oldViewProps->onError)) {
      swiftPart.setOnError(newViewProps.onError.get());
    }

    // Update hybridRef if it changed
    if (oldViewProps == nullptr
          ? newViewProps.hybridRef.isProvided()
          : !newViewProps.hybridRef.hasSameValue(oldViewProps->hybridRef)) {
      // hybridRef changed - call it with new this
      const auto& maybeFunc = newViewProps.hybridRef.get();
      if (maybeFunc.has_value()) {
        maybeFunc.value()(_hybridView);
      }
    }

    swiftPart.afterUpdate();
  }

  // 3. Continue in base class
  [super updateProps:props oldProps:oldProps];
}

+ (BOOL)shouldBeRecycled {
  return NitroVideo::NitroVideoAutolinking::isNitroVideoViewRecyclable();
}

- (void)prepareForRecycle {
  [self notifyOnDropView];
  [super prepareForRecycle];
  NitroVideo::HybridNitroVideoViewSpec_cxx& swiftPart = _hybridView->getSwiftPart();
  swiftPart.maybePrepareForRecycle();
}

#ifdef ENABLE_RCT_COMPONENT_VIEW_INVALIDATE
- (void)invalidate {
  [self notifyOnDropView];
  [super invalidate];
}
#endif

@end
